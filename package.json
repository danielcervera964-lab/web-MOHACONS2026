{
  "name": "mohacons-web",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build"
  },
  "dependencies": {
    "resend": "^4.1.0"
  }
}
